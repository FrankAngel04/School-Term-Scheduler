<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="C971.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:C971.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="SchoolTermScheduler"
    Shell.BackgroundColor="#0078D4" 
    Shell.ForegroundColor="White"  
    Shell.TitleColor="White"  
    Shell.DisabledColor="Gray" 
    Shell.UnselectedColor="White" > 
    
    <ShellContent
        Title="Term Scheduler"
        ContentTemplate="{DataTemplate pages:TermScheduler}"
        Route="TermScheduler" />

</Shell>